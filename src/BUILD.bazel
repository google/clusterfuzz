load("@bazel_gazelle//:def.bzl", "gazelle")

# gazelle:exclude third_party

gazelle(
    name = "gazelle",
    prefix = "clusterfuzz",
)

py_library(
    name = "clusterfuzz",
    deps = [
        "//python/crash_analysis/stack_parsing",
    ],
    srcs_version = "PY2ONLY",
)
